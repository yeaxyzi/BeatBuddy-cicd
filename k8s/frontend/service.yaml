apiVersion: v1
kind: Service
metadata:
  name: beatbuddy-frontend
  namespace: beatbuddy
spec:
  selector:
    app: beatbuddy-frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
